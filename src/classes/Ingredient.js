class Ingredient {
  constructor(id, name, cost, units, amount) {
    this.id = id
    this.name = name
    this.estimatedCostInCents = cost
    this.units = units
    this.amount = amount
  }
}

export default Ingredient
