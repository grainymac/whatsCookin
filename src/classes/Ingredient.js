class Ingredient {
  constructor(id, name, cost, amount, unit) {
    this.id = id
    this.name = name
    this.estimatedCostInCents = cost
    this.amount = amount
    this.unit = unit
  }
}

export default Ingredient
